# 全局搜索设计

## 概述

全局搜索提供两种模式：

- **普通搜索**：基于 SQLite FTS5 全文索引，分词匹配，适合关键词检索。
- **正则搜索**：直接扫描业务表原文，支持正则元字符，适合精确结构匹配（如 `^//\s+`、`TODO(?!:)`）。

结果以两列卡片展示，每卡最多 3 行 snippet，保留原始换行，命中片段 `<mark>` 高亮。点击结果跳转原文并高亮命中位置。

---

## 基础实现

### 1. 数据层：FTS5 索引

- 虚拟表 `search_fts(id, type, content, metadata, tokenize='unicode61')`。
- `type`：`document` / `question` / `answer` / `tag`。
- `content`：经 `segmentText()`（`Intl.Segmenter` 切词后空格 join）处理的扁平文本——**索引用，换行已丢失**。
- `metadata`：JSON，存关联 id、计数等展示辅助字段。
- 索引维护：文档/问题/回答/标签增删改时调用 `SearchService.updateXxx/deleteXxx` 增量更新；`rebuildIndex` 全量重建。所有写操作调用 `invalidateSearchCache()` 清缓存。

### 2. 普通搜索（FTS5）

入口 `SearchService.querySearch(searchText, limit)`：

1. `segmentText(searchText)` 分词，`escapeQuery` 去 `"` `*`，拼成 `"词"*` 的 FTS5 查询。
2. `SELECT ... snippet(search_fts, 2, '<mark>', '</mark>', '...', 32) FROM search_fts WHERE MATCH ? ORDER BY rank LIMIT limit*4`。
3. **取原文**：`fetchOriginalContents(rows)` 按 `type` 批量回查业务表（`documents.title` / `questions.text` / `answers.content` / `tags.name`），用原文替代 FTS5 的扁平 content，**保留换行**。
4. `groupByType` 按类型分组，每类取前 `limit` 条，生成 snippet。
5. 缓存：`searchCache`，key = `s:${dbId}:${searchText}:${limit}`。

### 3. 正则搜索

入口 `SearchService.querySearchRegex(searchText, limit, complexExecutor?)`：

- **字面量降级**（`isLiteralPattern` 为真）：走 `searchLiteral`，按表用 SQL `INSTR(LOWER(col), ?)` 过滤并 `LIMIT`，命中行由 `literalSnippet` 生成片段。避免全表扫描。
- **复杂正则**：调用注入的 `complexExecutor`（worker），未注入时回退同步 `searchRegexFromDb`。

#### Worker 异步化

- 纯函数 `searchRegexFromDb(db, searchText, limit)`（`electron/regexSearch.ts`）：主进程与 worker 共用，全表扫描原文逐行 `match`。
- `electron/searchWorker.ts`：`workerData` 收 `dbPath`，worker 内独立 `new DatabaseSync(dbPath)`（句柄不可跨线程），连接懒创建复用，只读。
- `electron/main.ts`：惰性建 worker，`requestId` + `Promise` + 30s 超时；失败/超时/worker 异常时**退化为同步 `searchRegexFromDb`**，结果不丢失。退出/关库时 `terminate`。

### 4. Snippet 生成（统一）

核心公共函数 `buildLineSnippet(text, matchIndex, matchLength)`（`electron/regexSearch.ts`，导出）：

- 以匹配区间为中心，取上下各 1 行（共 3 行）。
- 匹配行：以匹配段为中心按字符半径（50）截取，`<mark>` 完整包裹。
- 上下文行：截前 100 字符，过长加 `...`。
- 片段转义 HTML（`escapeHtml`），防 `<script>` 等吞掉 `<mark>`。

所有路径统一调用：

- 正则：`makeSnippet` → `buildLineSnippet(text, match.index, match[0].length)`
- 字面量：`literalSnippet` 中 `indexOf` 定位后 → `buildLineSnippet`
- FTS5：`ftsSnippetToLineSnippet(originalContent, ftsSnippet)` 从 SQLite snippet 提取 `<mark>` 词，在原文 `indexOf` 定位后 → `buildLineSnippet`；定位失败回退原始 snippet。
- FTS5 snippet 为空兜底：`extractSnippet` 按首个关键字定位后 → `buildLineSnippet`。

### 5. 前端展示与高亮

- **SearchModal**：搜索框 + 正则切换（`isRegex`）+ 结果列表。
- **SearchResults**：两列 grid 卡片，`white-space: pre-line` 保留换行；snippet 服务端已包 `<mark>`，前端直接 `v-html`。
- **点击跳转**（`App.vue handleSearchSelect`）：`regexMode` 全链路透传（SearchResults → SearchModal → App）。
  - `scrollToQuestion`：在答案 DOM 文本节点中定位命中，临时包 `<mark>` 并滚动到视口 1/3 处，3s 后还原。正则模式用 `RegExp.exec` 定位，普通模式用 `indexOf`。
  - `setHighlightText(text, regexMode)`：`documentStore` 存 `highlightText` + `highlightRegexMode`，3s 有效。
- **持久高亮**（`MarkdownRenderer`）：`highlightSearchText(html, keyword, regexMode)`。
  - 正则模式：`new RegExp(keyword, 'gi')` 匹配，命中转义后包 `<span class="search-highlight">`。
  - 普通模式：按空白拆词逐个字面量高亮，兼容 FTS5 分词匹配。

---

## 后续优化设计

### 性能

1. **中文分词质量**：当前 `Intl.Segmenter` 对中文是按字/词切，FTS5 `unicode61` 分词器对中文支持有限，长中文可能需要 `unicode61 remove_diacritics 2` 或接入结巴/zmdict 分词器，提升召回率。
2. **FTS5 rank 调优**：当前默认 `ORDER BY rank`，可按 type 加权（如问题标题 > 回答正文）或引入 bm25 自定义权重。
3. **limit 策略**：`LIMIT limit*4` 再分组截断，可改为每 type 子查询分别 limit，减少无效扫描。
4. **worker 池**：当前单 worker 串行，复杂正则并发请求会排队；可按需扩到 2~3 个 worker（注意 db 只读句柄可多开）。

### 体验

1. **snippet 上下文可调**：当前固定 3 行、半径 50，可按卡片宽度动态调整，或提供"展开"查看完整上下文。
2. **多词高亮**：普通模式已拆词高亮，但 snippet 中只 `<mark>` 首个匹配词，可高亮全部命中。
3. **滚动定位精度**：`scrollToQuestion` 依赖文本节点未被拆分，若命中跨 markdown 标签边界会定位失败，可改用 Range/Selection API。
4. **正则高亮与 snippet 一致**：当前 snippet 的 `<mark>` 由服务端 `buildLineSnippet` 生成，跳转后高亮由前端正则重新匹配，两者对跨行匹配的处理可能不一致，可统一由服务端输出命中位置。

### 架构

1. **SearchService 职责拆分**：当前类同时管索引维护、普通搜索、正则搜索、snippet 生成，可按 DDD 拆为 `SearchIndexService`（索引）、`SearchQueryService`（查询编排）、`SnippetBuilder`（片段）。
2. **snippet 策略可配置**：`buildLineSnippet` 的行数、半径、上下文行数硬编码，可抽为策略对象，支持文档/问题/回答差异化展示。
3. **缓存粒度**：`searchCache` 是进程内 Map，无大小上限，可加 LRU 或按 db 关闭时清空（当前 `invalidateSearchCache` 只在写时调用）。
