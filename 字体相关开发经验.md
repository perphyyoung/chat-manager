# 字体相关开发经验

本文件沉淀字体相关开发经验：系统字体枚举、等宽判定、中文字体名映射与设置持久化。

## 系统字体枚举（queryLocalFonts）

- 官方 API：`window.queryLocalFonts()`（Local Font Access API），**Chromium 104+** 支持，返回本机全部字体（`FontData`：family / fullName / postscriptName / style）。
- 约束：需 **secure context + 用户手势**（调用会弹授权框）；拒绝授权或不可用时抛出异常。
- 框架无关：纯 Web API，不绑定 Vue/Electron。Electron 与 Tauri2(Windows/WebView2) 可用；**Tauri2 的 macOS(WKWebView) 与 Linux(WebKitGTK) 不支持**，须回退候选表。
- 用法：打开设置时调用一次，缓存结果为 `Set<family>`；与字体名映射并行加载。
- 注意：授权弹窗只在**有用户手势的调用**中出现（如设置页打开），无手势时可能直接失败。

## 判断字体是否等宽（官方无 API）

官方不提供"字体是否等宽"的 API（`FontData` 无此属性，CSS 无此能力），采用三层判定：

1. **名称关键词命中即等宽**（最稳，不依赖渲染）：family 含 `Mono`/`Menlo`/`Consolas`/`Courier`/`Code`/`Console`/`JetBrains`/`Fira`/`Cascadia`/`Term` 等特征词。如"LXGW WenKai Mono"命中 `Mono`。
2. **`document.fonts.load('16px "family"')` 预加载后 canvas 采样**：等宽字体所有字符宽度相同，采样 `i`/`W`/`0`/`l` 四字符宽度全部相等才判等宽。预加载避免 canvas 对未激活字体（尤其用户级安装字体，如 `%LOCALAPPDATA%\Fonts`）静默回退默认字体导致的误判。
3. 均不命中按非等宽处理（只影响分类位置，不影响功能）。

**避坑**：`document.fonts.check()` **不能**检测字体是否已安装——MDN 明确"不存在的字体 check() 也返回 true"。

## 字体中文名映射（数据目录 toml）

- 位置：数据目录（`dataDirManager.getDbDir()`）下 `font-name-map.toml`，随数据目录备份/迁移，设置页"打开文件夹"可找到。
- 格式：每行 `"英文族名" = "中文名"`，`#` 开头的行为注释。
- 解析：**逐行解析，注释/空行/不规范行跳过**，单个坏行不影响其余行；文件不存在时主进程自动写入默认模板；读取失败回退内置默认映射。
- 显示：优先中文名并附英文原文（如"微软雅黑 (Microsoft YaHei)"），无映射保持英文。API 无法取本地化名，映射表是实际可行方案。

## 设置持久化与应用

- 状态：`settings` store 维护 `fontFamily` / `monoFontFamily`，选择即保存（与深色模式一致）。
- 两项设置与适用范围：
  - **全部字体**：界面正文等非代码区域，候选含全部字体（标准 + 等宽），非代码区域也可选用等宽字体。
  - **等宽字体**：编辑器与代码块等代码区域，候选仅等宽字体。
- 应用：写入 `document.documentElement.style` 的 CSS 变量 `--font-family` / `--font-mono`。
- 回退：未设置时 CSS 变量取默认栈（正文：`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif`；等宽：`"Monaco", "Menlo", "Consolas", "Courier New", monospace`），行为不变。
