# 开发经验

## CodeMirror

### 撤销/重做

1. **使用 `shallowRef`** 存储 EditorView，避免 Vue 响应式系统干扰
2. **初始化时清空历史**：通过 Compartment 重新配置 history
3. **按钮失去焦点**：点击撤销/重做按钮会导致编辑器失去焦点，`ctrl+y` 会失效

## 代码块语言标识

### 不要在源码里插入标记，渲染层读取 lang 显示徽标

1. **初版方案（已废弃）**：右键"添加语言标识"在代码块第一行插入 `// language: xxx` 注释，用正则匹配围栏代码改源码，侵入性强，且标记注释会污染复制内容
2. **踩过的坑**：正则需兼容 ``` 与语言名之间的空格/Tab 与 CRLF/LF（` ``` json` 写法会导致仅匹配 ```lang 或 ```\n 的正则失败），且多次插入需幂等判断首行是否已有标记
3. **优化方案**：渲染层（markdown.ts renderer.code）直接读取围栏代码已声明的 lang，生成 `<span class="code-block-lang">` 徽标显示在代码块左上角，不改源码、零侵入；右键菜单已删除该功能

## 代码块行号

### 行号列与代码列分离（不切行方案）

1. **不要对 Prism 高亮后的 HTML 按 `\n` 切行**：嵌套语言（vue/html 的 `<script>`、`<style>`）的 token span 会跨行（开标签留在 `<script>` 行、闭标签出现在后续行），切行会把未闭合嵌套拆散，`</script>` 等内容脱离行容器落入行号区域
2. **行号列独立生成**（行业规范，同 Prism 官方 line-numbers 插件思路）：代码保持 Prism 原样不切行，行号列按物理行数生成，flex 双列并排；`user-select: none` + `aria-hidden` 保证不影响复制
3. **行号数以渲染物理行为准**：嵌套语言会合并物理行（如空行与 `</script>` 挤在同一行），行号与源码行号有偏差，但视觉逐行对齐才是目标
4. **行号计数与代码内容都要 `replace(/\n$/, "")`**：Prism 输出以 `\n` 结尾，只去一处会导致行号列与代码列差 1 行错位
5. **逐行对齐前提**：行号列与代码列 font-size、line-height 必须一致，应抽成组合选择器（`pre code, .code-lines`）共用一份定义，调行距只改一处、两列同步；代码列 `flex: 1 1 auto; min-width: 0` 实现横向滚动时行号列固定

### 踩过的坑

- 逐行 `<span class="code-line">` + CSS counter 伪元素生成行号可行，但前提是每行完整闭合——嵌套语言跨行 span 不满足，故弃用
- grid 布局会让 Prism token span 变成 grid item 逐格重排（"按空格分行"），flex 双列无此问题

