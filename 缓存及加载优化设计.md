# 缓存及加载优化设计

## 背景与目标

优化文档列表与详情的数据加载，减少不必要的长时间文本（answers/questions）传输与重复组装配对，提升列表首屏与选中切换的响应速度。

原实现的主要问题：

- 文档列表加载会返回含 answers 长文本的完整数据，列表体量大时开销高；主进程对每个文档做嵌套查询（N+1）。
- 每次选中/切换文档都重新组装配对，同一文档被反复加载。
- `documents` 数组既当列表摘要又当详情来源，任意改动都要整份替换，写回逻辑分散重复。

## 优化思路

### 1. 列表与详情分离

列表只需要轻量信息，详情才需要完整内容，二者分开加载：

- 新增摘要模型 `DocumentListItem`：仅含 `id / title / createdAt / updatedAt / questionCount / tags`，不含长文本。
- 新增 `document:listDocuments` IPC 返回摘要数组；仓储层提供 `listSummaries()`，应用服务提供 `loadAllSummaries()`。
- 文档列表改为只加载摘要；完整详情由独立的当前文档状态持有，不再混在列表数组里。

### 2. 查询由嵌套改为批量

主进程将原先"逐文档嵌套查询"收敛为 3 条批量 JOIN 查询，一次性取出所有文档、问题、回答、标签并分组，替代 N+1 查询。

### 3. 主进程详情缓存

主进程维护文档详情缓存，以 `updated_at` 作为指纹：文档未变更时直接返回缓存，变更时重新组装并更新。写操作都会刷新 `updated_at`，因此缓存按指纹自然失效，无需额外的清理逻辑。

### 4. 渲染进程缓存与预取

- 渲染进程维护已加载过的文档详情缓存，选中时优先命中，未命中才向主进程拉取并写入缓存。
- 选中某文档后，后台静默预取相邻文档缓存，不阻塞当前操作，失败忽略。
- 将多处重复的"获取文档 + 替换列表项"写回逻辑收敛为统一方法：写操作后只刷新该文档，同步更新其详情缓存、当前选中状态与列表摘要条目。

## 涉及文件

| 层 | 文件 |
| --- | --- |
| 类型 | `src/types/dto.ts`（DocumentListItem）、`src/types/api.ts`（listDocuments 类型） |
| 主进程 | `electron/main.ts`（批量查询、listDocuments、详情缓存） |
| IPC 暴露 | `electron/preload.ts` |
| 仓储 | `src/domain/repositories/DocumentRepository.ts`、`src/infrastructure/storage/SqliteDocumentRepository.ts` |
| 服务 | `src/application/services/DocumentApplicationService.ts` |
| 状态 | `src/presentation/stores/document.ts`（摘要列表、详情缓存、统一写回、预取） |
| 组件 | `src/presentation/components/Document/DocumentItem.vue`（改为摘要计数） |

## 一致性保证

- 主进程缓存以 `updated_at` 为指纹，写后自然失效。
- 渲染进程详情缓存在每次写回时同步刷新，与主进程保持一致。
- 列表摘要条目由详情派生，写回后同步更新标题、更新时间与问题计数。
